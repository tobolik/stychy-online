# Handoff – 2026-06-22

## Větev
`fix-results-display` (NENÍ v master). Zpracování 13 připomínek k záznamníku Štychy.
Plán: docs/plans/2026-06-21-stychy-pripominky.md

## Hotovo a commitnuto
1. `26421cf` – Rychlé fixy: P0 CSS proměnné (--primary/--card-bg/--text/--bg-main),
   items 1 (dropdown), 2 (dedupe hráčů), 3 (reset balíčků), 5 (text tlačítka),
   6b (pořadí výsledků), 7 (select), 8+9 (trumf), 12 (ESC). Odsouhlaseno.
2. `81b5ada` – Metaplán D mobil: items 4,6,6c,13 + zmenšené karty trumfu
   (tvar 3/4, min-height 120px), větší číslovky a hodnoty karet na mobilu.
   Odsouhlaseno na DevTools emulaci.

## Rozpracováno (NECOMMITNUTO)
Metaplán E – roast statistiky (item 10 + 11), v offline-recorder.html:
- HOTOVO E1: calculateGameStats – přidán total_score + position do stat objektu,
  maxOverbid, odebrány staré single-winner award sorty, return má awards (pole)
  + maxOverbid místo perfectRounds.
- HOTOVO E2: getAwardPool() (14 ocenění: 8 roast + 6 pozitivních) + assignAwards()
  (greedy unikátní přiřazení, poražený první na roast, normalizace min-max).
- ZBÝVÁ E3: renderStats sekce 2 → karta na hráče místo fixních 4 award karet.
- ZBÝVÁ E4: renderStats sekce 1 → nahradit perfectRounds kartu za maxOverbid
  ("Nejvíc přehlášené kolo").
- ⚠️ POZOR: renderStats ještě odkazuje na zrušené stats.perfectRounds a
  stats.awards.sniper.name → statistiky se v pracovním stromě teď NEVYKRESLÍ.
  Commity výše jsou OK, rozbité je jen necommitnuté WIP.

## Další kroky
1. Dodělat E3 + E4 v renderStats, ověřit na seedovaných datech.
2. Version bump (v1.3.4 → v1.4.0) + build-date ve footeru.
3. HITL: vizuální/on-device kontrola celé dávky.
4. Rozhodnout o merge fix-results-display → master (= nasazení na produkci přes SFTP).

## Testovací prostředí
docker compose -f docker-compose.dev.yml up -d --build
Web http://localhost:8088, login test/test1234, "Testovací hra" (4 hráči,
1 dokončené + 1 rozehrané kolo). Docker soubory v .gitignore (nedeployují se).
Produkce se nasazuje jen z master přes SFTP (.github/workflows/deploy.yml).

## Session summary
- Načteno repo, odhalen nesoulad: screenshoty = větev fix-results-display, ne master.
- Brainstorm + BaC-planner + BaC-architect + Miro-replanning na celý plán.
- Naimplementováno 11 z 13 připomínek (rychlé fixy + mobilní layout).
- Postaveno testovací Docker prostředí (PHP+MySQL) se seedovanými daty.
- Zbývá dokončit roast statistiky (metaplán E, ~2 úpravy v renderStats).
